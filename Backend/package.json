{
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "postgres": "^3.4.3"
  },
  "scripts": {
    "build": "npx",
    "start": "npx node src/index.js",
    "reset": "psql -U development -d listful < ./src/database/schemas/create.sql && psql -U development -d listful < ./src/database/schemas/development.sql"
  }
}
